# 更新日志

## v1.1.0 — 2026-09-14

### 新增：末位数字 0-9 均匀性检验

自然数据的**末位数字**应近似在 0-9 上均匀分布（各 10%）。新增检验：

- **剔除明显取整的数据**（这是末位检验成立的前提）：报告形态下有 ≥2 个连续尾零的金额，
  如 `100,000,000.00`、`500,000`、`23,000` —— 它们的末位是被舍入固定死的，
  纳入会系统性扭曲分布。剔除数量与占比会在报告中列明
- **统一检验框架**：卡方拟合优度（df=9）+ 蒙特卡洛 MAD 检验（20000 次，不依赖固定阈值）
  + 各数字 z 统计量；digits 从 1-9 泛化为可配置（首位 1-9 / 末位 0-9）
- **输出**：报告新增「三、末位数字：0-9 均匀性检验」章节（含 0-9 明细表）、
  `results/last_digit.png` 分布图（含 10% 期望参考线）、CSV 中的末位数据集行
- **界面**：新增「末位数字分布图」标签页；检验结论面板追加末位数字区块；
  分布明细下拉框中可直接选末位数据集（列头由「首位数字」改为「数字」以兼容 0-9）

### 实测（星宇股份 601799，13 期报告）

| 项目 | 结果 |
|---|---|
| 候选金额 | 1400 个 |
| 剔除明显取整 | 91 个（6.5%） |
| 有效样本 | n = 1309 |
| 卡方检验 | χ² = 15.84（df=9），p = 0.070 |
| 蒙特卡洛 p | 0.134 |
| 结论 | **不拒绝 0-9 均匀分布** |

TCL科技（n=569）同样不拒绝（p=0.578）。首位数字检验结果不受影响（星宇仍为 n=1400、χ²=7.33）。

## v1.0.1 — 2026-09-14

### 修复

- **同时披露中英文版的公司（如 TCL科技 000100）只列到英文版，导致几乎抽不到数据**
  （用户实测：选多期财报但样本量 n=2）。报告列表按公告日期倒序去重时，
  英文版通常披露更晚、排在前面，把同一期的中文版挤掉了；而英文版报表是
  「千元整数」口径且为英文标题。现在同一报告期**中文版优先**，英文版仅在
  没有中文版时才使用，并在报告列表标注「（英文版）」
- **支持英文版报表**：新增英文报表标题识别（Consolidated Balance Sheet /
  Income Statement / Cash Flow Statement，含 `(Continued)`）与英文校验标签
  （Total assets / Total liabilities / Total equity）
- **支持千元、万元等整数口径金额**：这类报表金额形如 `23,007,773`（无小数位），
  此前只认"两位小数"因而几乎抽不到数；现在同时接受带千分位的整数金额，
  纯整数（附注编号、页码、年份）仍被排除
- **诊断信息更准确**：不再把"外文版报告"误报为"扫描版 PDF"，并给出对应建议

### 回归

- 沪 / 深 / 北 / 科创 8 家公司版式回归 8/8 通过（新增 TCL科技 用例）
- 星宇股份交付口径结果不变（n=1400，χ²=7.33，p=0.502）
- 英文版报表实测：三张表全部定位、恒等式 1/1 通过

## v1.0.0 — 2026-09-14

首个正式版本。

### 功能

- **公司检索**：从巨潮资讯网（cninfo，证监会指定信息披露平台）搜索任意 A 股上市公司，
  自动列出年报 / 半年报 / 一季报 / 三季报，同一期的"正文/全文"自动优先全文
- **数据获取与解析**：下载官方 PDF → 抽取文本 → 定位合并资产负债表 / 利润表 / 现金流量表并抽取金额
- **Benford 检验**：卡方拟合优度、Nigrini MAD 阈值、KS 型统计量、蒙特卡洛 MAD（不依赖固定阈值）
- **质量自证**：会计恒等式（资产=负债+权益）+ 跨报告期衔接逐项校验，并列出差异明细
- **多口径稳健性**：9 个数据集（主口径 / 含同比列 / 去重唯一值 / 绝对值阈值 / 分表 / 分时段）
- **三种用法**：tkinter 图形界面、命令行批处理、可复用 Python 包
- **版式自适应**：带序号标题、纯数字附注列、章节引用列、表格页被抽成一行、项目名跨行断开等
- **导出**：Markdown 报告、PNG 分布图、CSV 明细、原始数据行

### 打包与分发

- `EconomyCheck-v1.0.0-win64-portable.zip` —— **便携包（推荐）**：解压即用，启动快，
  内含 `EconomyCheck.exe`（图形界面）与 `EconomyCheck-CLI.exe`（命令行）
- `EconomyCheck-v1.0.0-onefile.zip` —— 单文件版：每个程序仅一个 exe，便于单独拷贝
  （启动时解压到 `%TEMP%`，慢几秒；系统限制临时目录时请改用便携包）
- `SHA256SUMS.txt` —— 两份压缩包的校验和

### 实测数据（星宇股份 601799，13 期定期报告）

| 项目 | 结果 |
|---|---|
| 主口径样本 | n = 1400 |
| 卡方检验 | χ² = 7.33（df=8），p = 0.502 |
| MAD | 0.0069（Nigrini：可接受） |
| 蒙特卡洛 MAD | p = 0.331 |
| 结论 | 符合 Benford 对数分布 |
| 会计恒等式 | 13/13 期通过 |
| 跨报告期衔接 | 902/933 项一致（96.7%） |
| 跨公司版式回归 | 7/7 通过（沪 / 深 / 北 / 科创） |

### 说明

- 数据来源于公开披露文件，解析结果可能存在误差，**不构成任何投资建议**
- 可执行文件未做代码签名，首次运行 Windows SmartScreen 可能提示"未知发布者"
- 需要联网：报告 PDF 从巨潮资讯网官方地址下载
